import React from "react";

const Navbar = props => (
  <nav className="navbar">
    <img
      alt="logo"
      src={require("../../Images/logo.png")}
      className="logoNav"
    />{" "}
  </nav>
);

export default Navbar;
